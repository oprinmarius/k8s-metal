---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.argo-cd.name }}
spec:
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
  destination:
    namespace: {{ .Values.argo-cd.namespace }}
    server: https://kubernetes.default.svc
  project: default
  source:
    chart: argo-cd
    repoURL: https://argoproj.github.io/argo-helm
    targetRevision: {{ .Values.argo-cd.version }}
    helm:
      valueFiles:
        - values.yaml
      values: |-
        {{- toYaml .Values.argo-cd.values | nindent 8 }}
